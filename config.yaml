name: GM Local Config
version: 1.0.0
schema: v1

models:
  - name: Local GM Chat
    provider: ollama
    model: llama3:latest

rules:
  - Keep answers concise and actionable.
  - Prefer markdown output.
  - When editing campaign files, preserve headings and wiki links.